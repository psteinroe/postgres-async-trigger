import { type FunctionsIndex } from "../../types";
import sampleTrigger from "./sample-trigger";

export const contactFunctions: FunctionsIndex = {
	functions: [],
	triggers: [sampleTrigger],
	repeated: [],
};
